[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[build.env]
NIXPACKS_JDK_VERSION = "21"
MAVEN_OPTS = "-Xmx192m -XX:MaxMetaspaceSize=128m -Xss512k -XX:+UseSerialGC"

[deploy]
startCommand = "java -jar backend/target/app.jar"
